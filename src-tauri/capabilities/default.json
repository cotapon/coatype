{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for CoAType",
  "windows": ["settings", "overlay"],
  "permissions": [
    "core:default",
    "shell:default",
    "fs:default",
    "dialog:default"
  ]
}
